import { FC, ReactElement } from "react"
import { List } from "rsuite"

const ClothList: FC = (): ReactElement => {
  // const []
  return (
    <div>
      <List />
    </div>
  )
}

export default ClothList
